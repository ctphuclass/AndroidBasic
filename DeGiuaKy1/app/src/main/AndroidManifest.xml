<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.degiuaky1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentActivity"></activity>
        <activity android:name=".CreateItemActivity" /> <!-- <activity android:name=".DemoTabControlActivity"></activity> -->
        <activity android:name=".UpdateChiTietActivity" />
        <activity android:name=".ChiTietActivity" />
        <activity android:name=".MonHocActivity" />
        <activity android:name=".DemoTabControlActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>